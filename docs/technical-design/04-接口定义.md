# llama.cpp 技术设计文档

## 第四部分：接口定义

### 4.1 公共 API 接口

#### 4.1.1 模型接口

```cpp
// include/llama.h

// 加载模型
LLAMA_API struct llama_model * llama_load_model_from_file(
    const char * path_model,
    struct llama_model_params params);

// 释放模型
LLAMA_API void llama_free_model(struct llama_model * model);

// 获取模型信息
LLAMA_API int64_t llama_model_size(const struct llama_model * model);
LLAMA_API int64_t llama_model_n_params(const struct llama_model * model);
LLAMA_API uint32_t llama_model_n_vocab(const struct llama_model * model);
LLAMA_API uint32_t llama_model_n_ctx_train(const struct llama_model * model);
LLAMA_API uint32_t llama_model_n_embd(const struct llama_model * model);
LLAMA_API int32_t llama_model_n_layer(const struct llama_model * model);
LLAMA_API uint32_t llama_model_n_head(const struct llama_model * model);

// 获取词汇表
LLAMA_API const struct llama_vocab * llama_model_get_vocab(
    const struct llama_model * model);

// 获取超参数
LLAMA_API const struct llama_hparams * llama_model_get_hparams(
    const struct llama_model * model);

// 获取架构
LLAMA_API enum llm_arch llama_model_arch(const struct llama_model * model);
LLAMA_API const char * llama_model_arch_name(const struct llama_model * model);
LLAMA_API const char * llama_model_desc(const struct llama_model * model);

// 获取量化信息
LLAMA_API uint64_t llama_model_quantize_used_bytes(const struct llama_model * model);
LLAMA_API uint64_t llama_model_quantize_total_bytes(const struct llama_model * model);
```

#### 4.1.2 上下文接口

```cpp
// 创建上下文
LLAMA_API struct llama_context * llama_init_from_model(
    const struct llama_model * model,
    struct llama_context_params params);

// 释放上下文
LLAMA_API void llama_free(struct llama_context * ctx);

// 获取模型引用
LLAMA_API const struct llama_model * llama_get_model(
    const struct llama_context * ctx);

// 获取上下文大小
LLAMA_API uint32_t llama_n_ctx(const struct llama_context * ctx);
LLAMA_API uint32_t llama_n_batch(const struct llama_context * ctx);
LLAMA_API uint32_t llama_n_ubatch(const struct llama_context * ctx);
LLAMA_API uint32_t llama_n_seq_max(const struct llama_context * ctx);

// 设置线程数
LLAMA_API void llama_set_n_threads(
    struct llama_context * ctx,
    uint32_t n_threads,
    uint32_t n_threads_batch);

// 获取内存使用
LLAMA_API size_t llama_get_mem_size(const struct llama_context * ctx);
LLAMA_API size_t llama_get_max_mem_size(const struct llama_context * ctx);

// 获取性能统计
LLAMA_API int64_t llama_get_time_us(const struct llama_context * ctx);
```

#### 4.1.3 推理接口

```cpp
// 解码批处理
LLAMA_API int32_t llama_decode(
    struct llama_context * ctx,
    struct llama_batch batch);

// 获取 logits
LLAMA_API float * llama_get_logits(struct llama_context * ctx);
LLAMA_API float * llama_get_logits_ith(struct llama_context * ctx, int32_t i);

// 获取嵌入
LLAMA_API float * llama_get_embeddings(struct llama_context * ctx);
LLAMA_API float * llama_get_embeddings_ith(struct llama_context * ctx, int32_t i);
LLAMA_API float * llama_get_embeddings_seq(
    struct llama_context * ctx,
    llama_seq_id seq_id);

// 采样
LLAMA_API llama_token llama_sampler_sample(
    struct llama_sampler * smpl,
    struct llama_context * ctx,
    int32_t idx);
```

#### 4.1.4 词汇表接口

```cpp
// Token 化
LLAMA_API int32_t llama_tokenize(
    const struct llama_vocab * vocab,
    const char * text,
    size_t text_len,
    llama_token * tokens,
    int32_t n_tokens_max,
    bool add_special,
    bool parse_special);

LLAMA_API int32_t llama_tokenize(
    const struct llama_vocab * vocab,
    const llama_token * tokens,
    int32_t n_tokens,
    char * text,
    size_t text_len_max,
    bool remove_special);

// 逆 Token 化
LLAMA_API int32_t llama_detokenize(
    const struct llama_vocab * vocab,
    const llama_token * tokens,
    int32_t n_tokens,
    char * text,
    size_t text_len_max,
    bool remove_special);

// 特殊 token
LLAMA_API llama_token llama_vocab_bos(const struct llama_vocab * vocab);
LLAMA_API llama_token llama_vocab_eos(const struct llama_vocab * vocab);
LLAMA_API llama_token llama_vocab_eot(const struct llama_vocab * vocab);
LLAMA_API llama_token llama_vocab_sep(const struct llama_vocab * vocab);
LLAMA_API llama_token llama_vocab_nl(const struct llama_vocab * vocab);
LLAMA_API llama_token llama_vocab_pad(const struct llama_vocab * vocab);

// Token 信息
LLAMA_API const char * llama_vocab_get_text(
    const struct llama_vocab * vocab,
    llama_token token);

LLAMA_API float llama_vocab_get_score(
    const struct llama_vocab * vocab,
    llama_token token);

LLAMA_API enum llama_token_type llama_vocab_get_type(
    const struct llama_vocab * vocab,
    llama_token token);
```

#### 4.1.5 批处理接口

```cpp
// 创建批处理
LLAMA_API struct llama_batch llama_batch_init(
    int32_t n_tokens,
    int32_t embd,
    int32_t n_seq_max);

// 释放批处理
LLAMA_API void llama_batch_free(struct llama_batch batch);

// 批处理辅助函数
LLAMA_API void llama_batch_add(
    struct llama_batch & batch,
    llama_token id,
    llama_pos pos,
    const std::vector<llama_seq_id> & seq_ids);
```

#### 4.1.6 状态保存/加载接口

```cpp
// 保存状态
LLAMA_API size_t llama_state_get_size(struct llama_context * ctx);
LLAMA_API size_t llama_state_get_data(
    struct llama_context * ctx,
    uint8_t * dst,
    size_t size);

// 加载状态
LLAMA_API size_t llama_state_set_data(
    struct llama_context * ctx,
    const uint8_t * src,
    size_t size);

// 保存到文件
LLAMA_API bool llama_state_save_file(
    struct llama_context * ctx,
    const char * path_session,
    const llama_token * tokens,
    size_t n_token);

// 从文件加载
LLAMA_API bool llama_state_load_file(
    struct llama_context * ctx,
    const char * path_session,
    llama_token * tokens_out,
    size_t * n_token_out);

// 序列状态
LLAMA_API size_t llama_state_seq_get_size(
    struct llama_context * ctx,
    llama_seq_id seq_id);

LLAMA_API size_t llama_state_seq_get_data(
    struct llama_context * ctx,
    uint8_t * dst,
    size_t size,
    llama_seq_id seq_id);

LLAMA_API size_t llama_state_seq_set_data(
    struct llama_context * ctx,
    const uint8_t * src,
    size_t size,
    llama_seq_id dest_seq_id);
```

### 4.2 采样器接口

#### 4.2.1 采样器链接口

```cpp
// 创建采样器链
LLAMA_API struct llama_sampler_chain * llama_sampler_chain_init(
    struct llama_sampler_chain_params params);

// 释放采样器链
LLAMA_API void llama_sampler_chain_free(struct llama_sampler_chain * chain);

// 添加采样器
LLAMA_API void llama_sampler_chain_add(
    struct llama_sampler_chain * chain,
    struct llama_sampler * smpl);

// 获取采样器
LLAMA_API struct llama_sampler * llama_sampler_chain_get(
    const struct llama_sampler_chain * chain,
    int32_t i);

// 移除采样器
LLAMA_API struct llama_sampler * llama_sampler_chain_remove(
    struct llama_sampler_chain * chain,
    int32_t i);

// 应用采样器链
LLAMA_API void llama_sampler_chain_apply(
    struct llama_sampler_chain * chain,
    float * logits,
    struct llama_token_data_array * cur_p);

// 接受 token
LLAMA_API void llama_sampler_chain_accept(
    struct llama_sampler_chain * chain,
    llama_token token);
```

#### 4.2.2 内置采样器

```cpp
// 温度采样器
LLAMA_API struct llama_sampler * llama_sampler_init_temp(float temp);

// Top-K 采样器
LLAMA_API struct llama_sampler * llama_sampler_init_top_k(int32_t k);

// Top-P 采样器（核采样）
LLAMA_API struct llama_sampler * llama_sampler_init_top_p(float p, size_t min_keep);

// Min-P 采样器
LLAMA_API struct llama_sampler * llama_sampler_init_min_p(float p, size_t min_keep);

// XTC 采样器
LLAMA_API struct llama_sampler * llama_sampler_init_xtc(
    float p,
    float t,
    size_t min_keep,
    uint32_t seed);

// 典型采样器
LLAMA_API struct llama_sampler * llama_sampler_init_typical(
    float p,
    size_t min_keep);

// 温度缩放采样器
LLAMA_API struct llama_sampler * llama_sampler_init_temp_ext(
    float t,
    float delta,
    float exponent);

// 重复惩罚采样器
LLAMA_API struct llama_sampler * llama_sampler_init_penalties(
    int32_t penalty_last_n,
    float penalty_repeat,
    float penalty_freq,
    float penalty_present);

// DRY 采样器
LLAMA_API struct llama_sampler * llama_sampler_init_dry(
    float dry_multiplier,
    float dry_base,
    int32_t dry_allowed_length,
    int32_t dry_penalty_last_n,
    const char ** seq_breakers,
    size_t num_breakers);

// 语法采样器
LLAMA_API struct llama_sampler * llama_sampler_init_grammar(
    const struct llama_vocab * vocab,
    const char * grammar_str,
    const char * grammar_root);

// 分布采样器
LLAMA_API struct llama_sampler * llama_sampler_init_dist(
    uint32_t seed);

// 无操作采样器（用于调试）
LLAMA_API struct llama_sampler * llama_sampler_init_none();
```

### 4.3 后端接口

#### 4.3.1 后端注册接口

```cpp
// ggml/include/ggml-backend.h

// 获取后端数量
LLAMA_API size_t ggml_backend_reg_get_count(void);

// 查找后端
LLAMA_API size_t ggml_backend_reg_find_by_name(const char * name);
LLAMA_API size_t ggml_backend_reg_find_by_location(const char * location);
LLAMA_API size_t ggml_backend_reg_find_by_type(enum ggml_backend_dev_type type);

// 遍历后端
LLAMA_API ggml_backend_reg_t ggml_backend_reg_get(size_t index);

// 后端信息
LLAMA_API const char * ggml_backend_reg_name(ggml_backend_reg_t reg);
LLAMA_API size_t ggml_backend_reg_get_device_count(ggml_backend_reg_t reg);
LLAMA_API ggml_backend_dev_t ggml_backend_reg_get_device(
    ggml_backend_reg_t reg,
    size_t index);

// 初始化后端
LLAMA_API ggml_backend_t ggml_backend_reg_init_backend_from_str(
    ggml_backend_reg_t reg,
    const char * params_str);

// 默认后端
LLAMA_API ggml_backend_t ggml_backend_cpu_init(void);
```

#### 4.3.2 后端设备接口

```cpp
// 设备信息
LLAMA_API const char * ggml_backend_dev_name(ggml_backend_dev_t dev);
LLAMA_API const char * ggml_backend_dev_description(ggml_backend_dev_t dev);
LLAMA_API enum ggml_backend_dev_type ggml_backend_dev_type(ggml_backend_dev_t dev);
LLAMA_API void ggml_backend_dev_memory(ggml_backend_dev_t dev, size_t * free, size_t * total);

// 初始化后端
LLAMA_API ggml_backend_t ggml_backend_dev_init(
    ggml_backend_dev_t dev,
    const char * params);

// 获取缓冲区类型
LLAMA_API ggml_backend_buffer_type_t ggml_backend_dev_buffer_type(
    ggml_backend_dev_t dev);

// 获取主机缓冲区类型
LLAMA_API ggml_backend_buffer_type_t ggml_backend_dev_host_buffer_type(
    ggml_backend_dev_t dev);

// 获取设备
LLAMA_API ggml_backend_dev_t ggml_backend_get_device(ggml_backend_t backend);
```

#### 4.3.3 缓冲区类型接口

```cpp
// 缓冲区类型信息
LLAMA_API const char * ggml_backend_buft_name(ggml_backend_buffer_type_t buft);
LLAMA_API ggml_backend_buffer_t ggml_backend_buft_alloc_buffer(
    ggml_backend_buffer_type_t buft,
    size_t size);
LLAMA_API size_t ggml_backend_buft_get_alignment(ggml_backend_buffer_type_t buft);
LLAMA_API size_t ggml_backend_buft_get_max_size(ggml_backend_buffer_type_t buft);
LLAMA_API size_t ggml_backend_buft_get_alloc_size(
    ggml_backend_buffer_type_t buft,
    const struct ggml_tensor * tensor);
LLAMA_API bool ggml_backend_buft_is_host(ggml_backend_buffer_type_t buft);
LLAMA_API ggml_backend_dev_t ggml_backend_buft_get_device(ggml_backend_buffer_type_t buft);
```

#### 4.3.4 缓冲区接口

```cpp
// 缓冲区操作
LLAMA_API const char * ggml_backend_buffer_name(ggml_backend_buffer_t buffer);
LLAMA_API void ggml_backend_buffer_free(ggml_backend_buffer_t buffer);
LLAMA_API void * ggml_backend_buffer_get_base(ggml_backend_buffer_t buffer);
LLAMA_API size_t ggml_backend_buffer_get_size(ggml_backend_buffer_t buffer);
LLAMA_API enum ggml_status ggml_backend_buffer_init_tensor(
    ggml_backend_buffer_t buffer,
    struct ggml_tensor * tensor);
LLAMA_API size_t ggml_backend_buffer_get_alignment(ggml_backend_buffer_t buffer);
LLAMA_API size_t ggml_backend_buffer_get_max_size(ggml_backend_buffer_t buffer);
LLAMA_API size_t ggml_backend_buffer_get_alloc_size(
    ggml_backend_buffer_t buffer,
    const struct ggml_tensor * tensor);
LLAMA_API void ggml_backend_buffer_clear(ggml_backend_buffer_t buffer, uint8_t value);
LLAMA_API bool ggml_backend_buffer_is_host(ggml_backend_buffer_t buffer);
LLAMA_API void ggml_backend_buffer_set_usage(
    ggml_backend_buffer_t buffer,
    enum ggml_backend_buffer_usage usage);
LLAMA_API enum ggml_backend_buffer_usage ggml_backend_buffer_get_usage(
    ggml_backend_buffer_t buffer);
LLAMA_API ggml_backend_buffer_type_t ggml_backend_buffer_get_type(
    ggml_backend_buffer_t buffer);
LLAMA_API void ggml_backend_buffer_reset(ggml_backend_buffer_t buffer);
```

#### 4.3.5 后端操作接口

```cpp
// 后端信息
LLAMA_API ggml_guid_t ggml_backend_guid(ggml_backend_t backend);
LLAMA_API const char * ggml_backend_name(ggml_backend_t backend);
LLAMA_API void ggml_backend_free(ggml_backend_t backend);

// 缓冲区分配
LLAMA_API ggml_backend_buffer_type_t ggml_backend_get_default_buffer_type(
    ggml_backend_t backend);
LLAMA_API ggml_backend_buffer_t ggml_backend_alloc_buffer(
    ggml_backend_t backend,
    size_t size);
LLAMA_API size_t ggml_backend_get_alignment(ggml_backend_t backend);
LLAMA_API size_t ggml_backend_get_max_size(ggml_backend_t backend);

// 张量操作
LLAMA_API void ggml_backend_tensor_set_async(
    ggml_backend_t backend,
    struct ggml_tensor * tensor,
    const void * data,
    size_t offset,
    size_t size);

LLAMA_API void ggml_backend_tensor_get_async(
    ggml_backend_t backend,
    const struct ggml_tensor * tensor,
    void * data,
    size_t offset,
    size_t size);

LLAMA_API void ggml_backend_tensor_set(
    struct ggml_tensor * tensor,
    const void * data,
    size_t offset,
    size_t size);

LLAMA_API void ggml_backend_tensor_get(
    const struct ggml_tensor * tensor,
    void * data,
    size_t offset,
    size_t size);

LLAMA_API void ggml_backend_tensor_memset(
    struct ggml_tensor * tensor,
    uint8_t value,
    size_t offset,
    size_t size);

// 同步
LLAMA_API void ggml_backend_synchronize(ggml_backend_t backend);

// 计算图
LLAMA_API ggml_backend_graph_plan_t ggml_backend_graph_plan_create(
    ggml_backend_t backend,
    struct ggml_cgraph * cgraph);

LLAMA_API void ggml_backend_graph_plan_free(
    ggml_backend_t backend,
    ggml_backend_graph_plan_t plan);

LLAMA_API enum ggml_status ggml_backend_graph_plan_compute(
    ggml_backend_t backend,
    ggml_backend_graph_plan_t plan);

LLAMA_API enum ggml_status ggml_backend_graph_compute(
    ggml_backend_t backend,
    struct ggml_cgraph * cgraph);

LLAMA_API enum ggml_status ggml_backend_graph_compute_async(
    ggml_backend_t backend,
    struct ggml_cgraph * cgraph);

// 操作支持检查
LLAMA_API bool ggml_backend_supports_op(
    ggml_backend_t backend,
    const struct ggml_tensor * op);

LLAMA_API bool ggml_backend_supports_buft(
    ggml_backend_t backend,
    ggml_backend_buffer_type_t buft);

LLAMA_API bool ggml_backend_offload_op(
    ggml_backend_t backend,
    const struct ggml_tensor * op);
```

### 4.4 内存接口

#### 4.4.1 内存对象接口

```cpp
// src/llama-memory.h

// 内存对象接口
struct llama_memory_i {
    // 类型信息
    const char * (*type_name)();

    // 生命周期
    void (*free)(llama_memory_t memory);

    // 清空
    void (*clear)(llama_memory_t memory, bool keep_graph);

    // 优化
    bool (*optimize)(llama_memory_t memory);

    // 应用批处理
    bool (*apply_batch)(
        llama_memory_t memory,
        llama_context * lctx,
        const llama_batch & batch,
        llama_kv_cache_view_update & view_update,
        llama_kv_cache_view_copies & view_copies);

    // 状态保存/加载
    size_t (*state_size)(const llama_memory_t memory);
    size_t (*state_read)(llama_memory_t memory, llama_io_read_i & reader);
    size_t (*state_write)(const llama_memory_t memory, llama_io_write_i & writer);
};
```

#### 4.4.2 KV 缓存视图接口

```cpp
// KV 缓存视图
struct llama_kv_cache_view {
    int32_t n_cells;          // 单元格总数
    int32_t n_seq_max;        // 最大序列数
    int32_t token_count;      // 已用 token 数

    // 单元格信息
    struct Cell {
        llama_pos pos;
        llama_seq_id seq_id;
        bool is_empty;
    };
    std::vector<Cell> cells;
};

// 获取 KV 缓存视图
LLAMA_API struct llama_kv_cache_view llama_kv_cache_view_init(
    const struct llama_context * ctx,
    int32_t n_seq_max);

LLAMA_API void llama_kv_cache_view_free(struct llama_kv_cache_view * view);

LLAMA_API bool llama_kv_cache_view_update(
    const struct llama_context * ctx,
    struct llama_kv_cache_view * view);
```

### 4.5 语法接口

#### 4.5.1 语法解析接口

```cpp
// src/llama-grammar.h

// 创建语法
LLAMA_API struct llama_grammar * llama_grammar_init(
    const llama_grammar_element ** rules,
    size_t n_rules,
    size_t start_rule_index);

// 释放语法
LLAMA_API void llama_grammar_free(struct llama_grammar * grammar);

// 克隆语法
LLAMA_API struct llama_grammar * llama_grammar_clone(
    const struct llama_grammar * grammar);

// 应用语法约束
LLAMA_API void llama_grammar_apply_constraints(
    struct llama_grammar * grammar,
    llama_token_data_array * candidates,
    const struct llama_vocab * vocab);

// 接受 token
LLAMA_API void llama_grammar_accept_token(
    struct llama_grammar * grammar,
    llama_token token);

// 重置语法状态
LLAMA_API void llama_grammar_reset(struct llama_grammar * grammar);
```

### 4.6 GGML 张量接口

#### 4.6.1 张量创建接口

```cpp
// ggml/include/ggml.h

// 创建新张量
GGML_API struct ggml_tensor * ggml_new_tensor(
    struct ggml_context * ctx,
    enum ggml_type type,
    int32_t n_dims,
    const int64_t * ne);

GGML_API struct ggml_tensor * ggml_new_tensor_1d(
    struct ggml_context * ctx,
    enum ggml_type type,
    int64_t ne0);

GGML_API struct ggml_tensor * ggml_new_tensor_2d(
    struct ggml_context * ctx,
    enum ggml_type type,
    int64_t ne0,
    int64_t ne1);

GGML_API struct ggml_tensor * ggml_new_tensor_3d(
    struct ggml_context * ctx,
    enum ggml_type type,
    int64_t ne0,
    int64_t ne1,
    int64_t ne2);

GGML_API struct ggml_tensor * ggml_new_tensor_4d(
    struct ggml_context * ctx,
    enum ggml_type type,
    int64_t ne0,
    int64_t ne1,
    int64_t ne2,
    int64_t ne3);

// 创建参数张量
GGML_API struct ggml_tensor * ggml_new_tensor_1d(
    struct ggml_context * ctx,
    enum ggml_type type,
    int64_t ne0);

GGML_API void ggml_set_param(
    struct ggml_context * ctx,
    struct ggml_tensor * tensor);
```

#### 4.6.2 张量操作接口

```cpp
// 一元操作
GGML_API struct ggml_tensor * ggml_relu(struct ggml_context * ctx, struct ggml_tensor * a);
GGML_API struct ggml_tensor * ggml_silu(struct ggml_context * ctx, struct ggml_tensor * a);
GGML_API struct ggml_tensor * ggml_gelu(struct ggml_context * ctx, struct ggml_tensor * a);
GGML_API struct ggml_tensor * ggml_tanh(struct ggml_context * ctx, struct ggml_tensor * a);
GGML_API struct ggml_tensor * ggml_sigmoid(struct ggml_context * ctx, struct ggml_tensor * a);

// 归一化
GGML_API struct ggml_tensor * ggml_rms_norm(struct ggml_context * ctx, struct ggml_tensor * a, float eps);
GGML_API struct ggml_tensor * ggml_group_norm(struct ggml_context * ctx, struct ggml_tensor * a, int32_t n_groups, float eps);
GGML_API struct ggml_tensor * ggml_layer_norm(struct ggml_context * ctx, struct ggml_tensor * a, float eps);

// 二元操作
GGML_API struct ggml_tensor * ggml_add(struct ggml_context * ctx, struct ggml_tensor * a, struct ggml_tensor * b);
GGML_API struct ggml_tensor * ggml_sub(struct ggml_context * ctx, struct ggml_tensor * a, struct ggml_tensor * b);
GGML_API struct ggml_tensor * ggml_mul(struct ggml_context * ctx, struct ggml_tensor * a, struct ggml_tensor * b);
GGML_API struct ggml_tensor * ggml_div(struct ggml_context * ctx, struct ggml_tensor * a, struct ggml_tensor * b);

// 矩阵乘法
GGML_API struct ggml_tensor * ggml_mul_mat(struct ggml_context * ctx, struct ggml_tensor * a, struct ggml_tensor * b);
GGML_API struct ggml_tensor * ggml_mul_mat_id(struct ggml_context * ctx, struct ggml_tensor * as, struct ggml_tensor * b, struct ggml_tensor * ids);

// 注意力
GGML_API struct ggml_tensor * ggml_flash_attn_ext(
    struct ggml_context * ctx,
    struct ggml_tensor * q,
    struct ggml_tensor * k,
    struct ggml_tensor * v,
    struct ggml_tensor * mask,
    float scale,
    float max_bias);

// RoPE
GGML_API struct ggml_tensor * ggml_rope(
    struct ggml_context * ctx,
    struct ggml_tensor * a,
    struct ggml_tensor * b,
    int32_t n_dims,
    int32_t mode,
    int32_t n_ctx);

// 池化
GGML_API struct ggml_tensor * ggml_pool_1d(
    struct ggml_context * ctx,
    struct ggml_tensor * a,
    enum ggml_op_pool op,
    int32_t k0,
    int32_t s0,
    int32_t p0);

// 转置
GGML_API struct ggml_tensor * ggml_transpose(struct ggml_context * ctx, struct ggml_tensor * a);

// 重塑
GGML_API struct ggml_tensor * ggml_reshape_2d(struct ggml_context * ctx, struct ggml_tensor * a, int64_t ne0, int64_t ne1);
GGML_API struct ggml_tensor * ggml_reshape_3d(struct ggml_context * ctx, struct ggml_tensor * a, int64_t ne0, int64_t ne1, int64_t ne2);
GGML_API struct ggml_tensor * ggml_reshape_4d(struct ggml_context * ctx, struct ggml_tensor * a, int64_t ne0, int64_t ne1, int64_t ne2, int64_t ne3);

// 复制
GGML_API struct ggml_tensor * ggml_copy(struct ggml_context * ctx, struct ggml_tensor * a, struct ggml_tensor * b);
```

#### 4.6.3 计算图接口

```cpp
// 创建计算图
GGML_API struct ggml_cgraph * ggml_new_graph(struct ggml_context * ctx);
GGML_API struct ggml_cgraph * ggml_new_graph_custom(
    struct ggml_context * ctx,
    size_t size,
    bool grads);

// 构建前向图
GGML_API void ggml_build_forward_expand(struct ggml_cgraph * cgraph, struct ggml_tensor * tensor);
GGML_API struct ggml_cgraph ggml_build_forward(struct ggml_tensor * tensor);

// 构建反向图
GGML_API struct ggml_cgraph ggml_build_backward(
    struct ggml_context * ctx,
    struct ggml_cgraph * gf,
    bool keep);

// 计算图
GGML_API void ggml_graph_compute(struct ggml_cgraph * cgraph, struct ggml_cplan * cplan);
GGML_API void ggml_graph_compute_with_ctx(
    struct ggml_context * ctx,
    struct ggml_cgraph * cgraph,
    int32_t n_threads);

// 计算计划
GGML_API struct ggml_cplan ggml_graph_plan(
    const struct ggml_cgraph * cgraph,
    int32_t n_threads,
    const struct ggml_threadpool * threadpool);
```

### 4.7 服务器接口

#### 4.7.1 HTTP API 端点

```
POST /completion
    - 文本补全请求
    - 支持流式响应

POST /chat/completions
    - OpenAI 兼容的聊天补全
    - 支持工具调用

POST /embedding
    - 嵌入向量生成

POST /tokenize
    - Token 化请求

POST /detokenize
    - 逆 Token 化请求

GET /models
    - 获取可用模型列表

GET /health
    - 健康检查

POST /slots
    - 管理推理槽位

POST /lora-adapters
    - 管理 LoRA 适配器
```

#### 4.7.2 请求/响应结构

```cpp
// 补全请求
struct completion_request {
    std::string prompt;
    std::vector<int> prompt_tokens;
    int n_predict;
    float temperature;
    int top_k;
    float top_p;
    float min_p;
    float repeat_penalty;
    bool stream;
    std::vector<std::string> stop;
    json grammar;
};

// 补全响应
struct completion_response {
    std::string content;
    std::vector<int> tokens;
    bool stopped_eos;
    bool stopped_word;
    bool stopped_limit;
    int predicted_tokens;
    std::string timing;
};

// 聊天请求
struct chat_completion_request {
    std::vector<chat_message> messages;
    std::string model;
    int n_predict;
    float temperature;
    bool stream;
    std::vector<tool> tools;
    json response_format;
};

// 聊天响应
struct chat_completion_response {
    std::string id;
    std::string object;
    int64_t created;
    std::string model;
    std::vector<choice> choices;
    usage usage;
};
```

### 4.8 量化接口

#### 4.8.1 量化参数

```cpp
// include/llama.h
struct llama_model_quantize_params {
    int32_t nthread;                     // 线程数
    enum ggml_type quant_type;           // 量化类型
    bool allow_requantize;               // 允许重新量化
    bool quantize_output_tensor;         // 量化输出张量
    bool only_copy;                      // 仅复制
    bool pure;                           // 纯量化
    void * imatrix;                      // 重要性矩阵
    void * kv_overrides;                 // KV 覆盖
};
```

#### 4.8.2 量化函数

```cpp
// 量化模型
LLAMA_API int llama_model_quantize(
    const char * fname_inp,
    const char * fname_out,
    const llama_model_quantize_params * params);

// 获取量化类型名称
LLAMA_API const char * llama_model_quantize_type_name(
    enum ggml_type type);
```
